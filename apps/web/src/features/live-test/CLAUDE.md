# apps/web/src/features/live-test/

The Phase D witnessed-test flow (DESIGN.md §14.5 / §14.6) plus the read-only recap (ADR 0022). Live tests reached from `/_authed/tests/$testId`; completed tests redirect to `/_authed/tests/$testId/recap`. All errors stream to the server per tap (ADR 0018); the finish endpoint reads them from the DB. During a live test the mushaf overlay is **always on** — each logged error immediately tints the corresponding word so the witness/student see the spatial picture alongside the error log on the right.

## Index

| File | What | When to read |
|---|---|---|
| `LiveTestRoute.tsx` | Two-pane layout: `<MushafPage>` left with prev/next page toolbar, `<ErrorLogPane>` right. Owns modal state for `ErrorLogModal` and `PostTestSummaryModal`. Loads the test row via direct Supabase select. Completed/abandoned tests redirect (effect-based, `replace: true`) to `/tests/:id/recap` | Touching the live-test surface or the completed-test redirect |
| `TestRecapView.tsx` | Read-only summary view (ADR 0022). Fetches `test` row + `error_log` rows via direct Supabase + existing student-select RLS. Renders metadata header (type · range · ended_at · duration · witness · rating badge), notes block, persisted `summary` via `PostTestSummaryView`, and the full logged-errors list. Shows a placeholder when `summary IS NULL` (tests completed before migration 0019) | Touching the recap surface or the historical-summary display |
| `ErrorLogModal.tsx` | Bottom-sheet modal on word-tap. 8 error-type chips, severity SegmentedControl, optional note. When `error_type === 'wrong_verse'`, shows an inline debounced QF Search field (radio-pick → `related_surah` + `related_ayah`) | Touching the error-logging UX or the wrong-verse differentiator |
| `ErrorLogPane.tsx` | Right pane: streaming `<LoggedErrorsList>` of logged errors, rating Select (options differ by test_type), notes textarea, "End test" button | Touching the live-test sidebar UI |
| `LoggedErrorsList.tsx` | Pure presentational error-list ScrollArea — per-error Paper with type/severity badges, ayah ref, wrong-verse intended target, optional teacher note. Used by both `ErrorLogPane` (live, `showTrashAffordance`) and `TestRecapView` (recap). Drives empty state from `emptyText` prop | Tweaking error-row presentation or the empty-state copy |
| `PostTestSummaryModal.tsx` | Modal wrapper around `<PostTestSummaryView>` with duration badge + Done button. Shown at the moment a test finishes (live flow) | Touching the post-test summary modal chrome |
| `PostTestSummaryView.tsx` | NEW / RECURRING / CLEARED sections backed by a `PostTestSummary` payload. Pure presentational; no Modal wrapper. Used by `PostTestSummaryModal` (live) and `TestRecapView` (historical) | Tweaking summary-section visuals or wording |
| `TestCreationModal.tsx` | Begin-Test modal: test_type + page range + witness name. Calls `POST /api/tests/create`, navigates to `/tests/$testId` on success. Trust nudge per ADR 0004 | Touching test creation UX |
| `useTestSession.ts` | Hook: holds `errors[]`, exposes `logError(input)` → `POST /api/tests/:id/error` and `finishTest(rating, notes)` → `POST /api/tests/:id/finish`. Invalidates cached `today_session`, `next_new_lesson`, mushaf overlays, etc. after finish | Adjusting the live-test data flow or post-finish invalidations |
| `useTestPages.ts` | `useTestPages(ranges)` derives the candidate page list (any page that overlaps any range) for prev/next navigation. Mirrors server `resolveTestRanges` (page-collection step) but doesn't compute fully-covered subset | Adjusting the page navigation order |
