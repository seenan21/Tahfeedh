# decisions/

Architecture Decision Records. Numbered, append-only. See `CLAUDE.md` at repo root for the ADR ritual.

## Index

| File | What | When to read |
|---|---|---|
| `0001-using-adr-format.md` | Establishes the ADR convention | Answering "why do we capture decisions at all?" |
| `0002-mushaf-data-source.md` | Picks api.quran.com v4 (open) over QUL SQLite | Touching `scripts/build-quran-data.ts` or page-data shape |
| `0003-per-page-qpc-v2-fonts.md` | 604 page-scoped fonts, one per mushaf page | Touching font loading, `quran-fonts.css`, or `MushafPage` font wiring |
| `0004-guest-witnessed-tests.md` | Adds guest_teacher test_mode + student-write RLS path | Building the test creation flow, RLS work on `test` |
| `0005-authed-layout-route.md` | Pathless `_authed` layout owns the gate + AppShell | Adding a protected route, changing the auth gate |
| `0006-half-page-numeric.md` | `pages_per_session_new` is NUMERIC(3,1), bounded 0.5–20 | Touching session-size settings or half-page algorithm |
| `0007-quran-index-build-artifact.md` | `quran-index.json` is the derived lookup for ranges | Touching range-expansion logic, juz-progress, or the index shape |
| `0008-onboarding-bulk-write-via-express.md` | Onboarding finish goes through Express + SQL function | Touching `commit_onboarding`, the finish endpoint, or RLS on derived tables |
| `0009-silkbackground-entry-points-only.md` | SilkBackground only on /login, /signup, / | Adding R3F surfaces or deciding where motion belongs |
| `0010-modernize-shell-visuals.md` | Allows same-anchor gradients + glass header inside the app shell | Reaching for a new gradient/depth treatment in app-shell screens |
| `0011-error-overlay-merge-at-render-time.md` | Errors merge at marker computation, not at storage — `error_log` stays an immutable occurrence list | Touching mushaf overlay rendering, marker computation, or the error detail modal |
| `0012-memorization-marking-endpoint.md` | **Superseded by 0015.** The marking endpoint was built then removed; the SQL fn is reserved for the post-M8 Edit Memorization flow | Wiring the future Settings recalibrate flow |
| `0013-next-new-lesson-rpc.md` | Queue 1 lives in Postgres as `next_new_lesson(uuid)` RPC | Touching the new-lesson slot on Today or the algorithm in M5 |
| `0014-hifz-direction-preference.md` | `hifz_direction` ('forward' Baqarah-first vs 'backward' Juz-Amma-first) on `student_settings`; `next_new_lesson` honors it | Touching Queue 1, onboarding step 1, or settings page direction toggle |
| `0015-memorization-status-test-driven.md` | Post-onboarding, `memorization_page.status` only changes via the post-test pipeline; the marking UI was removed | Resisting "let me just mark this page" UI temptations; planning the future Edit-Memorization flow |
| `0016-mushaf-rendering-fix.md` | Mushaf line layout uses `direction: rtl` + justified flex; no `row-reverse` (it caused a BiDi double-reversal that rendered LTR) | Touching `MushafPage` layout, debugging RTL/justification issues |
| `0017-post-test-pipeline-one-fat-fn.md` | Post-test pipeline is one SECURITY DEFINER PL/pgSQL function `submit_test(uuid, jsonb)` invoked by Express, mirroring `commit_onboarding` | Touching the post-test pipeline, mastery promotion (M5), or fail-downgrade |
| `0018-streaming-error-inserts.md` | Errors stream via `POST /api/tests/:id/error` per tap (not batched on finish) — durable against tab crashes | Touching the live-test session hook or error_log write path |
| `0019-overlay-client-side-merge.md` | `getOverlayMarkers` runs client-side from cached `error_location_stats`; no dedicated RPC | Touching overlay rendering, heatmap/colored modes, error detail modal |
| `0020-frozen-daily-session.md` | Today's session plan is persisted in `daily_session` and frozen for the day; explicit "Load next session" CTA inserts the next index | Touching the session machine, Today plan card, or revision ordering |
| `0021-tests-landing-shows-recent-history.md` | Tests landing shows a 30-day activity sparkline + the last 15 completed tests as clickable rows | Touching the tests landing layout or history surfaces |
| `0022-test-recap-route-and-persisted-summary.md` | `/tests/:id/recap` read-only route + `test.summary jsonb` persisted by `submit_test` so historical NEW/RECURRING/CLEARED stays truthful (extends ADR 0017) | Touching the recap surface, the post-test pipeline, or the live-test "already completed" redirect |
| `0023-error-detail-modal-design.md` | M7 error detail modal lists every individual `error_log` occurrence at the tapped location; `cleared = true` rows are hidden by default behind a "ghost errors — show" reveal (implemented by ADR 0026) | Building the error detail modal in M7, touching ghost-error visibility |
| `0024-m5-algorithm-completion.md` | M5 algorithm closure: mastery promotion at 5 consecutive clean strong-pass revision tests; fail resets Queue 2 stage to 1 only (no status downgrade); Queue 3 priority uses the DESIGN.md §7.2 formula minus mutashabihat_penalty (M9). Migration 0020 | Touching the algorithm, mastery threshold, fail behavior, or revision-queue ordering |
| `0025-progress-route-rename.md` | M7 reframe: `/timeline` renamed to `/progress`. Three reusable cards (Forecast / ActivityStats / RevisionHealth) accepting `studentId?` so M6 drill-in reuses them | Touching the Progress route, sidebar label, or adding student-facing analytics |
| `0026-error-detail-modal-implementation.md` | M7 implementation of ADR 0023. Modal lives in `apps/web/src/mushaf/ErrorDetailModal.tsx`, wired to `_authed.mushaf.tsx`. Reuses the parent's `error_location_stats` for ghost classification (no second fetch, no schema change) | Touching the error detail modal, ghost reveal mechanics, or extending it to another mushaf surface |
| `0027-teacher-directory-layout.md` | M6 teacher Students route is a directory (groups as folders + Ungrouped); inline group CRUD via existing RLS — no `/groups` route. Drill-in reuses M7 cards via `studentId` prop. `TestCreationModal` extended with `mode='enrolled_teacher'`; server `/api/tests/create` enrolled_teacher branch implemented | Touching the teacher directory, drill-in shape, group mechanics, or the enrolled_teacher test-create branch |
| `0028-invite-code-flip-and-classroom-tab.md` | M6 enrollment direction flipped: teacher generates an 8-char Crockford code (24h TTL, reusable), student joins via the new `/classroom` route. Drops `student_code`; adds `teacher_invite_code` + `get_or_create`/`rotate`/`leave_teacher` RPCs. `enroll_via_code` semantics flipped (caller is now student). New students land Ungrouped | Touching invite codes, the join flow, the Classroom tab, or `enroll_via_code` semantics |
| `0029-teacher-directory-polish.md` | Teacher directory polish: drops today-status badge from rows (was misread as enrollment-pending); adds Supabase Realtime on `enrollment` so joins appear live (first realtime surface, migration 0022); refactors `StudentRow` to a native `<div role="button">` for whole-row clickability + keyboard nav; swaps `Move` icon → `FolderInput` | Touching the teacher directory rows, the realtime pattern, or adding new realtime subscriptions |
| `0030-student-reads-teacher-display-name.md` | Mirror RLS policy on `app_user` so students can read the row of any teacher they have an active enrollment with. Closes the "Unnamed teacher" bug in the Classroom tab. Symmetric to the existing `is_my_student()` direction. Migration 0023 | Touching `app_user` RLS, cross-role visibility, or adding columns that shouldn't be visible across roles |
| `0031-teacher-tests-landing-and-students-route-fix.md` | (a) Renames `_authed.students.tsx` → `_authed.students.index.tsx` so it stops acting as a layout parent of `$studentId` (drill-in now actually renders); (b) `/tests` branches on `user.role` — teacher sees a `TeacherTestsHistory` list (administered tests with student names, click → recap), no self-test CTA, no sparkline. Drill-in remains the only test-creation entry point | Touching the `/tests` route, adding role-branched landings, or restructuring the students route tree |
| `0032-teacher-drill-in-expansion.md` | Drill-in surface expanded with viewer-aware reuse: `ForecastCard.isOwnView` (swaps "Adjust in Settings" copy), `ActivityStatsCard.viewerTeacherId` (filters tests + relabels "Tests taken with you"), `SessionPlanCard.readOnly` (hides interactive footer), and cross-route reuse of `today/JuzProgressBar` + `today/SessionPlanCard`. Adds pages-left annotation under incomplete juz cells (applies to student `/today` too). CTA → honey.7, back link → prominent button | Touching the drill-in layout, adding more viewer-context props, or extending today components to new surfaces |
| `0033-teacher-reads-student-session-and-all-tests.md` | Two RLS/RPC openings so the teacher drill-in works: (a) new `test_teacher_select_student` policy lets teachers SELECT all tests of enrolled students (writes still gated to teacher-administered); (b) `today_session(p_student_id)` guard relaxed to allow teacher-of-student. `load_next_session` + `next_new_lesson` stay locked to the student. Migration 0024 | Touching test RLS, the session-machine RPCs, or extending teacher read access to other student tables |
| `0034-error-type-scope-split.md` | Partitions the 8 error types by fixed scope. Word-scope: `tajweed`, `pronunciation`, `omission`, `addition`, `mismatch`. Verse-scope: `wrong_verse`, `forgotten_verse`, `hesitation`. `WORD_SCOPE_ERROR_TYPES` + `VERSE_SCOPE_ERROR_TYPES` + `scopeOfErrorType()` exported from `@tahfeedh/shared`. `logErrorSchema` validates scope/type match. Patches DESIGN.md §9.2 + §9.7 Case 3 | Adding a new error type, touching the `ErrorLogModal` chip list, or any code that branches on word vs verse scope |
| `0035-verse-tap-and-whole-verse-band.md` | Verse-end glyph (۝) becomes a first-class tap target. `MushafPage` gets `onVerseNumberTap`. Live test: verse-end tap opens `ErrorLogModal` with verse-scope chips only. My Mushaf: verse-end tap opens `ErrorDetailModal` in drill-up mode (query drops `word_position` filter; modal renders Verse-scope + per-word groups). New `.wordVerseBand` class tints every word of a verse-scope-marked ayah ~8%. Word markers still dominate visually. Patches DESIGN.md §9.7 (whole-verse rendering + tap-behavior inversion) | Touching the verse-end tap behavior, drill-up modal, or whole-verse visual treatment |
| `0038-submit-test-populates-memorization-verse.md` | Closes the deferred gap from ADR 0037. `submit_test` now UPSERTs `memorization_verse` rows for every (page, surah, ayah) of every fully-covered page (from new `coveredPageAyahs` payload field, emitted by `apps/server/src/pipelines/post-test/resolve.ts`). Tested pages now appear in Queue 2 with proper stage-machine cadence instead of Queue 2b's flat fallback. Runs unconditionally (any test type, pass or fail). Migration 0026 | Touching the post-test pipeline, page→ayah row maintenance, or the Queue 2 join |
| `0037-any-pass-advances-and-revision-fallback.md` | Two fixes (migration 0025): (a) `submit_test` page promotion is now UPSERT (was UPDATE WHERE in_progress, which matched zero rows for any page beyond the onboarding-seeded one) and fires on any pass rating (`strong_pass` / `excellent` / `good` / `pass_needs_practice`), not only `strong_pass`. Ayahs also enter Queue 2 stage 1 on any pass. (b) New Queue 2b in `_compute_session_plan` catches memorized pages with no stage progression yet (e.g. onboarding-memorized), ordered by page_number. Queue order: Q2 → Q2b → Q3. Partially supersedes ADR 0024's "only strong_pass promotes" point | Touching session advancement, promotion semantics, or the revision-queue ordering |
| `0036-witness-attribution-on-recent-tests.md` | Drill-in's Recent tests now tags each row "Witnessed by you" (sage, Eye icon, clickable) / "Self-test" / "Another teacher" (gray, Lock icon, non-clickable + tooltip explaining recap is witness-only). Companion to ADR 0033 which keeps `error_log` RLS narrowly gated — UI prevents dead-end navigation into recaps with empty error logs | Touching the recent-tests list rendering, the witness-attribution helper, or extending witness-aware gating to other surfaces |
| `0041-mushaf-toolbar-surah-jump.md` | Mushaf reader toolbar becomes a 3-col CSS grid: searchable surah `Select` (left, matches id / Latin / Arabic), centered prev/page-juz/next flipper, existing Show-errors + Jump-to-page on the right. Select value derives from `quranIndex.pages[N].surah_start` so the dropdown follows prev/next/jump | Touching the mushaf reader toolbar, the surah-jump UX, or the toolbar grid breakpoints |
| `0042-verse-audio-husary-everyayah.md` | `ErrorDetailModal` gets a verse-audio pill at the top: Play/Pause + 0.5×–2× speed control. Source is the EveryAyah Ḥusary 128kbps CDN (no auth, no proxy). New `VerseAudioPlayer.tsx`; resets on surah/ayah change; `<audio onError>` falls back to disabled + "unavailable" label without breaking the modal | Touching verse-audio playback, the modal player, or extending playback to another surface |
| `0043-today-tab-renamed-home.md` | Label-only rename: sidebar nav `Today / اليوم` → `Home / الرئيسية`; Today route's hero H1/H2 updated to match. Route path stays `/today` — no callsite churn (precedent ADR 0025 for `/progress`) | Touching the landing-tab label, the hero copy, or considering a path rename |
| `0044-qf-scope-empirical-set.md` | Probed QF `/oauth2/auth` per-scope to find what this client_id actually has. Granted: `bookmark`, `goal`, `streak.read` (also `openid`, `offline_access`, `content`). Denied: `note.create`, `reading_session.create`, `profile`. `SCOPES` trimmed; `pushNote` removed; `qf/notes.ts` deleted | Touching the QF OAuth scope list, adding a new QF feature that needs a non-granted scope, or restoring notes sync after the scope is granted |
| `0045-verse-tap-modal-and-explicit-bookmarks.md` | My Mushaf collapses to one tap target — any tap on a verse opens `VerseDetailModal`. New body: `VerseStrip` (per-verse mushaf-style render with marker tints; `WordSpan` extracted) + `VerseAudioPlayer` (gains Stop button) + `VerseBookmarkButton` + the existing error history. Bookmarks become user-explicit: `pushBookmark` → `addBookmark` (drops `isReading:true`, returns boolean); writes route to a lazy-created per-user `Tahfeedh` collection on QF (in-memory cache, falls back to `__default__`); new `POST /api/qf-user/bookmarks`; automatic pushes in `onboarding.ts` + `tests.ts` removed. Disconnected users see a contextual Connect CTA in the bookmark slot. Reverses 0023, 0026, 0040; subsumes 0035 drill-up | Touching the My Mushaf tap behaviour, the verse modal contents, bookmark wiring, the Tahfeedh-collection resolver, or contextual Connect surfaces |
| `0046-error-severity-removed.md` | `error_log.severity` dropped (migration 0027). `ErrorSeverity` type + zod field + UI badges + SegmentedControl all removed. Heat intensity already used `occurrence_count / (1 + tests_since_last_occurrence)` — no math change. Teacher notes preserved | Touching the error model, adding a new error attribute, or extending `OccurrenceRow` |
| `0047-drop-mastered-status.md` | `memorization_status` collapses to `in_progress \| memorized`. `mastered` enum value remains in pg_type (Postgres can't DROP VALUE) but no code writes it. `mastered_at` column dropped. UI sweep across `JuzProgressBar`, `MushafGrid`, `PageDetailsPanel`, etc. Supersedes ADR 0024's mastery rule | Touching memorization status filters, page badges, mastered-tier UI, or thinking about a future mastery tier |
| `0048-collapse-test-ratings.md` | `test_rating` collapses from 6 values to `pass \| repeat`. Same enum for both `newly_memorized` and `revision`. Historical rows migrated (`strong_pass`/`excellent`/`good`/`pass_needs_practice` → `pass`; `needs_work`/`fail` → `repeat`). `ErrorLogPane.RATING_OPTIONS`, all `ratingLabel`/`ratingColor` switches, `isPassingRating` simplified. Migration 0028 (ADD VALUE) + 0029 (UPDATE + function rewrites) | Touching test ratings, rating-display UI, or pass-rate computation |
| `0049-two-stage-recent-revision.md` | Recent revision drops from 3 stages to 2. Stage 1 → +1d → stage 2 → +3d → graduate. `repeat` at any stage resets to stage 1 +1d. Existing stage-3 ayahs migrated to graduated. CHECK constraint enforces `recent_stage IN (1,2)` | Touching the stage machine, ready_at intervals, or graduation rules |
| `0050-unified-revision-priority-scoring.md` | Drops the Q2 → Q2b → Q3 layered model. Single UNION ALL across recent + old + never-tested pages with one score each, top N wins. Recent score: `60 − (stage−1)×25 + sessions_overdue×15`. Old score: DESIGN.md §7.2 formula (minus mutashabihat). Never-tested folded into old via onboarding 30-day backdate. `revision_kinds text[]` added to `daily_session` + RPC outputs; `SessionPlanCard` renders "Recent" / "Older" sub-headers. Supersedes ADR 0024 (Q2+Q3 sequential) and ADR 0037 (Q2b tier) | Touching revision scoring, the queue model, or sub-header rendering |
| `0051-default-revision-pages-three.md` | `student_settings.pages_per_session_revision` DEFAULT changed from 5 to 3. Existing rows untouched. Step 3 preselects 3 with a sub-line nudge. `_compute_session_plan` coalesce + state.ts initial + hydrate fallback all aligned at 3 | Touching the revision-pages default, onboarding step 3 presets, or settings defaults |
| `0052-teacher-views-student-mushaf.md` | Extracts `_authed.mushaf.tsx` body into `mushaf/StudentMushafSurface.tsx` with `viewerRole: 'self' \| 'teacher'`. New `_authed.students.$studentId.mushaf.tsx` reuses the surface for teachers; drill-in renamed to `.index.tsx` (per ADR 0031 precedent). "View mushaf" CTA on drill-in header. localStorage last-page key scoped per viewer-context | Extending the mushaf surface to a new viewer, adding write affordances inside the mushaf, or wiring more teacher read-only views |
| `0053-live-test-historical-overlay-opt-in.md` | Live-test toolbar gets a "Show historical errors" Switch (default OFF, teacher-only — `test.teacher_id === user.id`). When ON, fetches `error_location_stats` and merges under the existing `liveOverlay`. Avoids witness-bias. Marker taps still route to `ErrorLogModal` only — no `VerseDetailModal` mount during a live test | Touching the live-test mushaf, the historical-vs-live merge, or extending witness-aware diagnostic surfaces |
