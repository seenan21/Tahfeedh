# packages/

Workspace packages shared between `apps/web` and `apps/server`.

## Index

| Path | What | When to read |
|---|---|---|
| `shared/` | `@tahfeedh/shared` — TypeScript types, Zod schemas, enum mirrors | Adding a type used by both web and server, or a request/response schema |
